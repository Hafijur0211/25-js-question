// 2. How to check if an object is an array or not? Provide some code.

function checkArray(elem) {
    //   console.log(Array.isArray(elem));
    returnArray.isArray(elem);
}

console.log(checkArray([]));
console.log(checkArray({}));
